name: platformsh-lite

project_files:
- config.platformsh-lite.yaml
- docker-compose.platformsh-lite.yaml
- platformsh-lite-addon-post-start.sh
- commands/web/platform
- commands/host/platform:tunnels

post_install_actions:
  - |
    #ddev-description: Remove deprecated host command platform ports if untouched
    [ -f commands/host/platform:ports ] && grep -q "#ddev-gener
    ated" commands/host/platform:ports && rm commands/host/platform:ports || true
